# מבחן חזרה לדוגמה בפייתון

כיתה י׳ | תרגול לקראת מבחן

שם התלמיד/ה: ____________________ כיתה: ________ תאריך: __________

הנחיות:
יש לענות בצורה מסודרת.
בשאלות קוד יש לכתוב קוד Python מלא וברור.
בשאלה שבה נדרשת טבלת מעקב, יש לבנות אותה לבד.
---

## חלק א: מושגים

20 נקודות, 2 נקודות לכל שאלה

### שאלה 1

מה ההבדל בין משתנה מסוג `int` לבין משתנה מסוג `str`?

תשובה: ________________________________________________________________

---

### שאלה 2

מה עושה הפקודה `len()` כאשר משתמשים בה על רשימה?

תשובה: ________________________________________________________________

---

### שאלה 3

מה ההבדל בין `append()` לבין `remove()` ברשימה?

תשובה: ________________________________________________________________

---

### שאלה 4

מה יודפס על ידי `range(1, 8, 2)`?

תשובה: ________________________________________________________________

---

### שאלה 5

מתי נעדיף להשתמש בלולאת `while` במקום בלולאת `for`?

תשובה: ________________________________________________________________

---

### שאלה 6

מה עושה הביטוי הבא?

```python
s[1:4]
```

תשובה: ________________________________________________________________

---

### שאלה 7

מה ההבדל בין `return` לבין `print()` בתוך פעולה?

תשובה: ________________________________________________________________

---

### שאלה 8

מה עושה הביטוי הבא?

```python
num % 10
```

תשובה: ________________________________________________________________

---

### שאלה 9

מה תפקיד הפעולה הבונה `__init__` במחלקה?

תשובה: ________________________________________________________________

---

### שאלה 10

מה ההבדל בין מחלקה לבין אובייקט?

תשובה: ________________________________________________________________

---

## חלק ב: טבלת מעקב

25 נקודות

### שאלה 11

חשב מה יודפס בסוף הריצה.
חובה לבנות טבלת מעקב לבד.

```python
total = 0
num = 8642

while num > 0:
    digit = num % 10
    if digit % 2 == 0:
        total += digit
    num //= 10

print(total)
```

תוצאה סופית: ________________________________

טבלת מעקב:

---

---

---

---

---

---

---

## חלק ג: תנאים ולולאות

25 נקודות
לא חובה טבלת מעקב

### שאלה 12

כתוב מה יודפס והסבר בקצרה למה.

```python
nums = [2, 5, 8, 11]

for i in range(len(nums)):
    if nums[i] % 2 == 0:
        nums[i] += 1
    else:
        nums[i] -= 1

print(nums)
```

מה יודפס? ________________________________

הסבר:

---

---

---

### שאלה 13

כתוב מה יודפס והסבר בקצרה למה.

```python
count = 0

for i in range(1, 10, 3):
    if i % 2 == 1:
        count += i

print(count)
```

מה יודפס? ________________________________

הסבר:

---

---

---

### שאלה 14

כתוב מה יודפס והסבר בקצרה למה.

```python
x = 5
y = 10

if x > 3 and y < 8:
    print("A")
elif x == 5 or y == 5:
    print("B")
else:
    print("C")
```

מה יודפס? ________________________________

הסבר:

---

---

---

## חלק ד: מחרוזות ורשימות

15 נקודות

### שאלה 15

כתוב מה יודפס.

```python
s = "python"
print(s[0])
print(s[1:4])
print(s[::-1])
```

מה יודפס?

---

---

---

---

### שאלה 16

כתוב מה יודפס.

```python
lst = [4, 8, 12]
lst.append(16)
lst.insert(2, 10)
lst.pop(1)

print(lst)
```

מה יודפס? ________________________________

---

## חלק ה: כתיבת קוד

15 נקודות

### שאלה 17

כתוב קוד שקולט מספר שלם ומדפיס את סכום הספרות שלו.
לדוגמה, עבור המספר `253` יודפס `10`.

---

---

---

---

---

---

---

### שאלה 18

כתוב פעולה בשם `is_even` שמקבלת מספר ומחזירה `True` אם המספר זוגי, אחרת מחזירה `False`.

---

---

---

---

---

### שאלה 19

כתוב מחלקה בשם `Car` הכוללת:

* פעולה בונה `__init__`
* תכונה לשם הרכב
* תכונה לשנת הייצור

לאחר מכן צור אובייקט אחד מהמחלקה והדפס את שם הרכב ואת שנת הייצור.


---

---
