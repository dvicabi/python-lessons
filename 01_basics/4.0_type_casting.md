<!-- DC_HEADER_START -->
<div align="center">

🐍 **Python Learning Series | סדרת לימודי פייתון**  
**Dvir Cabessa | דביר קבסה**  
© 2025 All Rights Reserved | כל הזכויות שמורות

</div>

---
<!-- DC_HEADER_END -->

# 📘 המרות בין סוגי נתונים - Type Casting

## ✨ הסבר

המרה היא פעולה שבה אנחנו משנים את סוג הנתון של ערך מסוים
בפייתון זה קורה הרבה כי לא תמיד סוג הנתון שיש לנו מתאים לפעולה שאנחנו רוצים לבצע

לדוגמה
קלט מהמשתמש תמיד מגיע כמחרוזת
אבל אם נרצה לחשב איתו נצטרך להמיר אותו למספר

את ההמרות מבצעים באמצעות פונקציות מובנות
`str()` `int()` `float()` `bool()`

---

## 🔄 המרות לפי יעד ההמרה

### 🔤 המרה ל-str

כל סוג ניתן להמרה למחרוזת ותמיד מצליח

```python
print(str("hello"))
print(str(10))
print(str(3.14))
print(str(True))
print(str(False))
```

---

### 🔢 המרה ל-int

המרה למספר שלם
שימו לב לאילו מקרים מצליחים ואילו לא

```python
print(int("123"))
# print(int("12a"))      # ValueError מחרוזת לא מספר חוקי

print(int(7))

print(int(3.9))          # חיתוך החלק העשרוני
print(int(-3.9))

print(int(True))         # 1
print(int(False))        # 0
```

---

### 🔢 המרה ל-float

המרה למספר עשרוני

```python
print(float("3.14"))
# print(float("abc"))    # ValueError מחרוזת לא מספר חוקי

print(float(7))

print(float(2.5))

print(float(True))       # 1.0
print(float(False))      # 0.0
```

---

### ✅ המרה ל-bool

בדיקה האם הערך נחשב אמת או שקר

```python
print(bool("hello"))
print(bool(""))           # מחרוזת ריקה

print(bool(7))
print(bool(0))

print(bool(2.5))
print(bool(0.0))

print(bool(True))
print(bool(False))
```

---

## 🧠 כללי זהב להמרות

* `str` תמיד מצליח
* `int` ו-`float` ממחרוזת עובדים רק אם זה מספר חוקי
* המרה מ-`float` ל-`int` חותכת ולא מעגלת
* `bool`

  * ריק או אפס → False
  * כל דבר אחר → True

---

<img src="טבלת-המרה.png" alt="טבלת המרה">

---

## ✅ בדוק את עצמך

0. מהי המרה ולמה צריך אותה
1. מה יקרה ב-`int("5.5")`
2. מה יהיה הערך של `bool(" ")`
3. למה `int(2.9)` לא מחזיר 3

<!-- DC_FOOTER_START -->
---

<div align="center">

✨ **Thank you for learning with Dvir Cabessa** ✨  
✨ **תודה שלמדתם עם דביר קבסה** ✨  

📘 *All Rights Reserved © Dvir Cabessa 2025*  
📘 *כל הזכויות שמורות © דביר קבסה 2025*  

🔗 *For educational purposes only – copying or distribution without permission is prohibited.*  
🔗 *החומר נועד לשימוש חינוכי בלבד — אין להעתיק או להפיץ ללא אישור.*

---

> _"Education is the art of awakening curiosity and guiding it toward creation."_  
> _"החינוך הוא אמנות המעירה את הסקרנות ומכוונת אותה ליצירה."_  
> — **Dvir Cabessa**

</div>
<!-- DC_FOOTER_END -->

```python

print(type(str("hello")))
print(type(str(10)))
print(type(str(3.14)))
print(type(str(True)))
print(type(str(False)))

print(type(int("123")))
# print(int("12a"))      # ValueError מחרוזת לא מספר חוקי

print(type(int(7)))

print(type(int(3.9)))          # חיתוך החלק העשרוני
print(type(int(-3.9)))

print(type(int(True)))         # 1
print(type(int(False)))        # 0

print(type(float("3.14")))
# print(float("abc"))    # ValueError מחרוזת לא מספר חוקי

print(type(float(7)))

print(type(float(2.5)))

print(type(float(True)))       # 1.0
print(type(float(False)))      # 0.0


print(type(bool("hello")))
print(type(bool("")))           # מחרוזת ריקה

print(type(bool(7)))
print(type(bool(0)))

print(type(bool(2.5)))
print(type(bool(0.0)))

print(type(bool(True)))
print(type(bool(False)))
```
